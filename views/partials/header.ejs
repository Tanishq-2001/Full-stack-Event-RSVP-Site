<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://kit.fontawesome.com/8da6764f1d.js" crossorigin="anonymous"></script>
    <title>FACE</title>
</head>
<body>
  
    <header>
        <div class="logo">
            <a href="/">
                <img src="/image/logo.png" alt="FACE">
            </a>
        </div>
        <div class="name">
            <h1>FACE</h1>
        </div>
        <nav>
            <ul>
                <% if(!user) { %>
                
                    <li><a href="/users/login">Login</a></li>
                    <li><a href="/users/new">Sign Up</a></li>
                <% } else { %>
                    <li><a href="/connections/newConnection">New Event</a></li>
                    <li><a href="/users/profile"><%= user.firstName %>'s Profile</a></li>
                    <li><a href="/users/logout">Logout</a></li>
                <% } %>
            </ul>
        </nav>
    </header>

    <nav class="main-nav">
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/connections">Events</a></li>
            
        </ul>
    </nav>
    


    <% if(errorMessages.length > 0) {%>
        <% errorMessages.forEach(message=>{%>
            <div class='error'>
                <%=message%>
            </div>
            <%});%>
                <%}%>
    
                    <% if(successMessages.length > 0) {%>
    
                        <% successMessages.forEach(message=>{%>
                            <div class='success'>
                                <%=message%>
                            </div>
                            <%});%>
                                <%}%>